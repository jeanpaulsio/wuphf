<% flash.each do |message_type, message| %>
  <div class="alert alert-dismissible fade show alert-<%= message_type %>">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
    <%= message %>
  </div>
<% end %>
