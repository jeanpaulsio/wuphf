<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand" href="/">WUPHF</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
       <%= link_to "<i class='fa fa-arrow-right'></i> Investors".html_safe, investors_path, class: "nav-link text-bold" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to "Account Settings",  edit_user_registration_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to("Log Out (#{current_user.name})", destroy_user_session_path, :method => :delete, class: "nav-link") %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to("Register", new_user_registration_path, class: "nav-link") %>
        </li>
        <li class="nav-item">
          <%= link_to("Sign In", new_user_session_path, class: "nav-link") %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
